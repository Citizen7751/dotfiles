#include <stdlib.h>
#include <unistd.h>

/* might just build
 * a bible verse
 * into the bar
 * 
 * #include <pthread.h>
 * FILE *bible = fopen("hely", "mód");
 * kiválasztás
 * kiprintelés
 * várás 1 percig
 * váltás egy másk random versre
 */

int main() {
	
	while(1) {
		
		system("clear");
		system("date");
		sleep(1);
	}
	
	return 0;
}
